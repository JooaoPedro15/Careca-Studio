// Partner Scout v2 type augmentation lives in src/types/electron.d.ts
// (consolidated to avoid `Window.careca` interface-merge conflicts).
// This file is kept as a placeholder so the plan's file structure matches.

export {}
